import { ApprovalsScreen } from "@/components/proc/approvals-screen";

/** Approvals queue — runs awaiting a decision before they're purchased. */
export default function ApprovalsPage() {
  return <ApprovalsScreen />;
}
