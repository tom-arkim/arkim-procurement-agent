import { HistoryScreen } from "@/components/proc/history-screen";

/** Past orders & prices — orders, spend by supplier, and the impact panel. */
export default function HistoryPage() {
  return <HistoryScreen />;
}
