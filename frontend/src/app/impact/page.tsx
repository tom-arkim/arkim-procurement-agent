import { ImpactScreen } from "@/components/proc/impact-screen";

/** Full "Your Arkim impact" panel — savings (measured), counts (fact), time (estimate). */
export default function ImpactPage() {
  return <ImpactScreen />;
}
