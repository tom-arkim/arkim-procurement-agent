[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["utils/procurement_agent/tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-v",
    "--tb=short",
    "--strict-config",
    "--strict-markers",
]
